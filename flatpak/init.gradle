allprojects {
    buildscript {
        repositories {
            flatDir {
                dirs "${rootDir}/dependencies"
            }
        }

        dependencies {
            classpath fileTree(dir: "${rootDir}/dependencies", include: ["*.jar"])
        }
    }
}
